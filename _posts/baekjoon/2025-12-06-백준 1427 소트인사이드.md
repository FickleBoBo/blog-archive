---
title: "[BaekJoon] 1427번 - 소트인사이드 [Java][C++]"
slug: baekjoon-1427
date: 2025-12-06
categories: [PS, BaekJoon]
tags: [sorting]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/1427)

---

## 1. 아이디어

주어진 $N$ 의 각 자릿수를 내림차순으로 정렬한 수를 출력하는 문제로 자릿수에 대한 계산을 해야 하기에 주어진 수를 문자열로 다루어서 역순 정렬을 구현했다.

---

## 2. 코드

### 1. 풀이 [Java]

주어진 수 $N$ 을 `char` 배열로 변환 후 정렬하고 역순으로 출력했다. `char` 타입의 경우 숫자의 비교 순서가 정수랑 똑같아서 가능하다.

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();

        char[] arr = br.readLine().toCharArray();
        Arrays.sort(arr);

        for (int i = arr.length - 1; i >= 0; i--) {
            sb.append(arr[i]);
        }

        System.out.println(sb);
    }
}
```

### 2. 풀이 [C++]

`string` 자체를 역순으로 정렬했다.

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    string s;
    cin >> s;

    sort(s.rbegin(), s.rend());
    cout << s;
}
```

---

## 3. 리뷰

없음.

---
