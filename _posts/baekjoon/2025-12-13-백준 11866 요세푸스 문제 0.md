---
title: "[BaekJoon] 11866번 - 요세푸스 문제 0 [Java][C++]"
slug: baekjoon-11866
date: 2025-12-13
categories: [PS, BaekJoon]
tags: [data structure, simulation, queue]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/11866)

---

## 1. 아이디어

$1$ 번부터 $N$ 번까지 $N$ 명의 사람이 원을 이루며 있을 때, $K$ 번째 사람을 제거하는 과정을 반복하는 문제다. 큐 자료구조를 활용하면 간단하게 해결할 수 있는데 큐의 맨 앞을 바라보며 $K$ 번째가 될 때까지 큐에서 꺼내서 큐 뒤에 넣다가 $K$ 번째면 큐에서 꺼내고 출력하는 과정을 반복하면 된다.

초기 큐에 $1$ 부터 $N$ 까지 순서대로 삽입한 후 큐가 완전히 비어 있기 전까지 반복문을 돌았다. 반복문 내부에서는 $K$ 번째 사람을 찾아야하므로 $K - 1$ 명을 큐의 앞에서 꺼내 큐의 뒤에 삽입했고, 이후 $K$ 번째 사람을 꺼내서 출력하는 과정을 반복했다.

---

## 2. 코드

### 1. 풀이 [Java]

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();
        StringTokenizer st = new StringTokenizer(br.readLine());

        int n = Integer.parseInt(st.nextToken());
        int k = Integer.parseInt(st.nextToken());

        Queue<Integer> q = new ArrayDeque<>();
        for (int i = 1; i <= n; i++) {
            q.offer(i);
        }

        sb.append("<");
        while (!q.isEmpty()) {
            for (int i = 1; i < k; i++) {
                q.offer(q.poll());
            }

            sb.append(q.poll());

            if (!q.isEmpty()) {
                sb.append(", ");
            }
        }
        sb.append(">");

        System.out.println(sb);
    }
}
```

### 2. 풀이 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n, k;
    cin >> n >> k;

    queue<int> q;
    for (int i = 1; i <= n; i++) {
        q.push(i);
    }

    cout << '<';
    while (!q.empty()) {
        for (int i = 1; i < k; i++) {
            q.push(q.front());
            q.pop();
        }

        cout << q.front();
        q.pop();

        if (!q.empty()) cout << ", ";
    }
    cout << '>';
}
```

---

## 3. 리뷰

없음.

---
