---
title: "[BaekJoon] 2562번 - 최댓값 [Java][C++]"
slug: baekjoon-2562
date: 2025-12-02
categories: [PS, BaekJoon]
tags: [warm up]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/2562)

---

## 1. 아이디어

서로 다른 9개의 자연수에 대해 최댓값과 등장 위치를 구하는 문제로 반복문을 활용하면 간단하게 해결할 수 있다.

---

## 2. 코드

### 1. 풀이 [Java]

```java
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        int max = 0;
        int idx = 0;
        for (int i = 1; i <= 9; i++) {
            int num = Integer.parseInt(br.readLine());

            if (num > max) {
                max = num;
                idx = i;
            }
        }

        System.out.println(max);
        System.out.println(idx);
    }
}
```

### 2. 풀이 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int mx = 0;
    int idx = 0;

    for (int i = 1; i <= 9; i++) {
        int num;
        cin >> num;

        if (num > mx) {
            mx = num;
            idx = i;
        }
    }

    cout << mx << '\n';
    cout << idx << '\n';
}
```

---

## 3. 리뷰

없음.

---
