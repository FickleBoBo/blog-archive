---
title: "[BaekJoon] 2693번 - N번째 큰 수 [Java][C++]"
slug: baekjoon-2693
date: 2026-01-27
categories: [PS, BaekJoon]
tags: [sorting]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/2693)

---

## 1. 아이디어

크기가 10인 각 배열에서 3번째로 큰 수를 출력하는 문제로 주어진 배열을 오름차순으로 정렬했을 때, 8번째에 위치한 원소를 찾으면 된다.

---

## 2. 코드

### 1. 풀이 [Java]

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();
        StringTokenizer st;

        int t = Integer.parseInt(br.readLine());
        while (t-- > 0) {
            int[] arr = new int[10];
            st = new StringTokenizer(br.readLine());
            for (int i = 0; i < 10; i++) {
                arr[i] = Integer.parseInt(st.nextToken());
            }
            Arrays.sort(arr);

            sb.append(arr[7]).append("\n");
        }

        System.out.print(sb);
    }
}
```

### 2. 풀이 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t;
    cin >> t;

    while (t--) {
        vector<int> v(10);
        for (int& x : v) cin >> x;
        sort(v.begin(), v.end());

        cout << v[7] << '\n';
    }
}
```

---

## 3. 리뷰

없음.

---
