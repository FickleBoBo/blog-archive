---
title: "[BaekJoon] 12904번 - A와 B [Java][C++]"
slug: baekjoon-12904
date: 2026-02-20
categories: [PS, BaekJoon]
tags: [greedy, string]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/12904)

---

## 1. 아이디어

두 가지 연산을 통해 변환할 수 있는지 판단하는 문제로 그리디 알고리즘으로 해결할 수 있다. $S$ 를 $T$ 로 바꾸는게 아니라 반대로 $T$ 를 $S$ 로 바꿔가면 되는데 $T$ 의 끝 자리가 $A$ 면 첫 번째 연산을 통해 현재 상태가 된 것이므로 끝 자리를 제거하고, 끝 자리가 $B$ 면 두 번째 연산을 통해 현재 상태가 된 것이므로 끝 자리를 제거하고 문자열을 뒤집으면 된다.

---

## 2. 코드

### 1. 풀이 [Java]

```java
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        char[] s = br.readLine().toCharArray();
        char[] t = br.readLine().toCharArray();
        int len = t.length;

        while (len > s.length) {
            char c = t[len - 1];

            if (c == 'A') {
                len--;
            } else {
                len--;
                reverse(t, len);
            }
        }

        for (int i = 0; i < len; i++) {
            if (s[i] != t[i]) {
                System.out.println(0);
                return;
            }
        }

        System.out.println(1);
    }

    static void reverse(char[] arr, int len) {
        for (int i = 0; i < len / 2; i++) {
            char tmp = arr[i];
            arr[i] = arr[len - 1 - i];
            arr[len - 1 - i] = tmp;
        }
    }
}
```

### 2. 풀이 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    string s, t;
    cin >> s >> t;
    int slen = s.size();
    int tlen = t.size();

    while (tlen > slen) {
        if (t[tlen - 1] == 'A') {
            tlen--;
        } else {
            tlen--;
            reverse(t.begin(), t.begin() + tlen);
        }
    }

    for (int i = 0; i < tlen; i++) {
        if (s[i] != t[i]) {
            cout << 0;
            return 0;
        }
    }

    cout << 1;
}
```

---

## 3. 리뷰

없음.

---
