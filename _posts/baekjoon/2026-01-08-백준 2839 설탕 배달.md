---
title: "[BaekJoon] 2839번 - 설탕 배달 [Java][C++]"
slug: baekjoon-2839
date: 2026-01-08
categories: [PS, BaekJoon]
tags: [greedy]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/2839)

---

## 1. 아이디어

3킬로그램 봉지와 5킬로그램 봉지로 $N$ 킬로그램을 맞춰야 하는 문제로 사용한 3킬로그램 봉지가 5개를 넘어가면 그 5개 대신 5킬로그램 봉지 3개를 써서 더 적은 수의 봉지로 $N$ 킬로그램을 맞출 수 있다. 따라서 3킬로그램 봉지를 하나도 안썼을 때부터 4개를 썼을 때까지 중 $N$ 킬로그램을 맞출 수 있는 경우가 존재하는지 찾으면 된다.

- 3킬로그램 봉지를 아예 안썼을 때는 $N$ 을 5로 나눈 나머지가 0이면 $N$ 킬로그램을 맞출 수 있는 것이다.
- 3킬로그램 봉지를 한 개 썼을 때는 $N - 3$ 을 5로 나눈 나머지가 0이면 $N$ 킬로그램을 맞출 수 있는 것이다.
- 3킬로그램 봉지를 두 개 썼을 때는 $N - 6$ 을 5로 나눈 나머지가 0이면 $N$ 킬로그램을 맞출 수 있는 것이다.
- 3킬로그램 봉지를 세 개 썼을 때는 $N - 9$ 을 5로 나눈 나머지가 0이면 $N$ 킬로그램을 맞출 수 있는 것이다.
- 3킬로그램 봉지를 네 개 썼을 때는 $N - 12$ 을 5로 나눈 나머지가 0이면 $N$ 킬로그램을 맞출 수 있는 것이다.
- 위 경우 모두 $N$ 킬로그램을 맞추지 못하면 3킬로그램 봉지와 5킬로그램 봉지로 $N$ 킬로그램을 맞출 수 없는 것이다.

---

## 2. 코드

### 1. 풀이 [Java]

```java
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        int n = Integer.parseInt(br.readLine());

        if (n % 5 == 0) {
            System.out.println(n / 5);
        } else if (n >= 3 && (n - 3) % 5 == 0) {
            System.out.println((n - 3) / 5 + 1);
        } else if (n >= 6 && (n - 6) % 5 == 0) {
            System.out.println((n - 6) / 5 + 2);
        } else if (n >= 9 && (n - 9) % 5 == 0) {
            System.out.println((n - 9) / 5 + 3);
        } else if (n >= 12 && (n - 12) % 5 == 0) {
            System.out.println((n - 12) / 5 + 4);
        } else {
            System.out.println(-1);
        }
    }
}
```

### 2. 풀이 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n;
    cin >> n;

    if (n % 5 == 0) {
        cout << n / 5;
    } else if (n >= 3 && (n - 3) % 5 == 0) {
        cout << (n - 3) / 5 + 1;
    } else if (n >= 6 && (n - 6) % 5 == 0) {
        cout << (n - 6) / 5 + 2;
    } else if (n >= 9 && (n - 9) % 5 == 0) {
        cout << (n - 9) / 5 + 3;
    } else if (n >= 12 && (n - 12) % 5 == 0) {
        cout << (n - 12) / 5 + 4;
    } else {
        cout << -1;
    }
}
```

---

## 3. 리뷰

없음.

---
