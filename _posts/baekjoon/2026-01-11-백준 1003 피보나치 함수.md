---
title: "[BaekJoon] 1003번 - 피보나치 함수 [Java][C++]"
slug: baekjoon-1003
date: 2026-01-11
categories: [PS, BaekJoon]
tags: [dp]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/1003)

---

## 1. 아이디어

$N$ 번째 피보나치 함수에서 0과 1의 출력 횟수를 구하는 문제로 다이나믹 프로그래밍을 활용하면 해결할 수 있다. $N$ 번째 피보나치 수는 $N - 1$ 번째 피보나치 수와 $N - 2$ 번째 피보나치 수를 재귀 호출하므로 0과 1의 출력 횟수 역시 두 수의 0과 1의 출력 횟수의 합과 같다.

---

## 2. 코드

### 1. 풀이 [Java]

```java
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();

        int[][] dp = new int[41][2];
        dp[0][0] = dp[1][1] = 1;
        for (int i = 2; i <= 40; i++) {
            dp[i][0] = dp[i - 1][0] + dp[i - 2][0];
            dp[i][1] = dp[i - 1][1] + dp[i - 2][1];
        }

        int t = Integer.parseInt(br.readLine());
        while (t-- > 0) {
            int n = Integer.parseInt(br.readLine());
            sb.append(dp[n][0]).append(" ").append(dp[n][1]).append("\n");
        }

        System.out.println(sb);
    }
}
```

### 2. 풀이 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

int dp[41][2];

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    dp[0][0] = dp[1][1] = 1;
    for (int i = 2; i < 41; i++) {
        dp[i][0] = dp[i - 1][0] + dp[i - 2][0];
        dp[i][1] = dp[i - 1][1] + dp[i - 2][1];
    }

    int t;
    cin >> t;

    while (t--) {
        int n;
        cin >> n;
        cout << dp[n][0] << ' ' << dp[n][1] << '\n';
    }
}
```

---

## 3. 리뷰

없음.

---
