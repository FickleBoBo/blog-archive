---
title: "[BaekJoon] 4134번 - 다음 소수 [Java][C++]"
slug: baekjoon-4134
date: 2025-12-18
categories: [PS, BaekJoon]
tags: [brute force, math, number theory]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/4134)

---

## 1. 아이디어

정수 $n$ 이 최대 $4 \times 10^9$ 일 때, $n$ 보다 크거나 같은 가장 작은 소수를 찾는 문제로 $n$ 이 소수가 아니면 $n + 1$ 을 한 후 소수인지 판정하는 과정을 반복하는 브루트 포스로 찾아나가면 해결할 수 있다. 이때 $n$ 의 제곱근까지만 나누어보는 것으로 소수를 판정하는 계산량을 줄일 수 있다.

---

## 2. 코드

### 1. 풀이 [Java]

$n$ 이 0 또는 1일 때는 2를 출력하도록 예외처리를 해줬다.

```java
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();

        int t = Integer.parseInt(br.readLine());
        while (t-- > 0) {
            long n = Long.parseLong(br.readLine());

            if (n <= 1) {
                sb.append("2\n");
                continue;
            }

            out:
            while (true) {
                for (long i = 2; i * i <= n; i++) {
                    if (n % i == 0) {
                        n++;
                        continue out;
                    }
                }

                sb.append(n).append("\n");
                break;
            }
        }

        System.out.print(sb);
    }
}
```

### 2. 풀이 [C++]

$n$ 이 0 또는 1일 때는 2를 출력하도록 예외처리를 해줬다.

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t;
    cin >> t;

    while (t--) {
        long long n;
        cin >> n;

        if (n <= 1) {
            cout << 2 << '\n';
            continue;
        }

        while (true) {
            bool isPrime = true;
            for (long long i = 2; i * i <= n; i++) {
                if (n % i == 0) {
                    isPrime = false;
                    n++;
                    break;
                }
            }

            if (isPrime) break;
        }

        cout << n << '\n';
    }
}
```

---

## 3. 리뷰

없음.

---
