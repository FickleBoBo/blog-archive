---
title: "[BaekJoon] 10610번 - 30 [Java][C++]"
slug: baekjoon-10610
date: 2026-02-24
categories: [PS, BaekJoon]
tags: [Unlinked]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/10610)

---

## 1. 아이디어

주어진 수가 30의 배수이면 주어진 수의 각 숫자들의 조합으로 가장 큰 수를 만들고 아니면 -1을 출력하는 문제로 30의 배수는 3의 배수이면서 10의 배수라는 점을 활용하면 해결할 수 있다. 3의 배수는 각 자릿수의 합이 3의 배수이면 3의 배수이고 10의 배수는 0을 가지고 있으면 10의 배수가 된다. 가장 큰 수는 각 자릿수의 숫자들에 대해 큰 수가 최대한 앞에 오게 배치하면 된다.

---

## 2. 코드

### 1. 풀이 [Java]

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();

        String s = br.readLine();
        int sum = 0;
        boolean has0 = false;

        for (char c : s.toCharArray()) {
            sum += c - '0';
            if (c == '0') has0 = true;
        }

        if (sum % 3 == 0 && has0) {
            char[] arr = s.toCharArray();
            Arrays.sort(arr);

            for (char c : arr) {
                sb.append(c);
            }
            sb.reverse();
        } else {
            sb.append(-1);
        }

        System.out.println(sb);
    }
}
```

### 2. 풀이 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    string s;
    cin >> s;

    int sum = 0;
    bool has0 = false;

    for (char c : s) {
        sum += c - '0';
        if (c == '0') has0 = true;
    }

    if (sum % 3 == 0 && has0) {
        sort(s.rbegin(), s.rend());
        cout << s;
    } else {
        cout << -1;
    }
}
```

---

## 3. 리뷰

없음.

---
