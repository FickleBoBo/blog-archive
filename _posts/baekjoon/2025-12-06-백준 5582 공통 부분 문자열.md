---
title: "[BaekJoon] 5582번 - 공통 부분 문자열 [Java][C++]"
slug: baekjoon-5582
date: 2025-12-06
categories: [PS, BaekJoon]
tags: [dp, string, lcs]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/5582)

---

## 1. 아이디어

두 문자열의 가장 긴 공통 부분 문자열을 찾는 문제로 최장 공통 부분 문자열 알고리즘(LCS)을 활용하면 해결할 수 있다. 최장 공통 부분 수열(Longest Common Subsequence)이 아니라 최장 공통 부분 문자열(Longest Common Substring)임에 주의해야 한다.

최댓값의 경우도 최장 공통 부분 수열은 dp 테이블의 마지막 인덱스에서 얻을 수 있지만, 최장 공통 부분 문자열은 어디에 저장됐을지 알 수 없다. 따라서 dp 테이블에서 가장 큰 값을 매번 갱신하는 방식으로 구현했다.

---

## 2. 코드

### 1. 풀이 [Java]

dp 테이블 앞에 패딩을 한 칸씩 줬다.

```java
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        char[] s1 = br.readLine().toCharArray();
        char[] s2 = br.readLine().toCharArray();
        int n = s1.length;
        int m = s2.length;

        int[][] dp = new int[1 + n][1 + m];
        int max = 0;

        for (int i = 1; i <= n; i++) {
            for (int j = 1; j <= m; j++) {
                if (s1[i - 1] == s2[j - 1]) {
                    dp[i][j] = dp[i - 1][j - 1] + 1;
                    max = Math.max(max, dp[i][j]);
                }
            }
        }

        System.out.println(max);
    }
}
```

### 2. 풀이 [C++]

dp 테이블 앞에 패딩을 한 칸씩 줬다.

```c++
#include <bits/stdc++.h>
using namespace std;

int dp[4001][4001];

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    string s1, s2;
    cin >> s1 >> s2;

    int n = s1.size();
    int m = s2.size();

    int mx = 0;

    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= m; j++) {
            if (s1[i - 1] == s2[j - 1]) {
                dp[i][j] = dp[i - 1][j - 1] + 1;
                mx = max(mx, dp[i][j]);
            }
        }
    }

    cout << mx;
}
```

---

## 3. 리뷰

없음.

---
