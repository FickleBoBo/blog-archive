---
title: "[BaekJoon] 1546번 - 평균 [Java][C++]"
slug: baekjoon-1546
date: 2025-12-02
categories: [PS, BaekJoon]
tags: [warm up]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/1546)

---

## 1. 아이디어

주어진 성적들을 배열로 받은 후 최댓값을 구한다. 이후 각 성적에 대해 $점수 / 최댓값 \times 100$ 으로 점수를 조작한 후 평균을 구하면 된다. 이때 정수 타입은 나머지가 버림 처리 될 수 있음에 주의해야 한다.

---

## 2. 코드

### 1. 풀이 [Java]

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;

        int n = Integer.parseInt(br.readLine());

        int[] arr = new int[n];
        int max = 0;

        st = new StringTokenizer(br.readLine());
        for (int i = 0; i < n; i++) {
            arr[i] = Integer.parseInt(st.nextToken());
            max = Math.max(max, arr[i]);
        }

        double sum = 0;
        for (int x : arr) {
            sum += (double) x / max * 100;
        }

        System.out.println(sum / n);
    }
}
```

### 2. 풀이 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n;
    cin >> n;

    vector<int> v(n);
    for (int& x : v) cin >> x;
    int mx = *max_element(v.begin(), v.end());

    double sum = 0;
    for (int x : v) {
        sum += (double)x / mx * 100;
    }

    cout << sum / n;
}
```

---

## 3. 리뷰

없음.

---
