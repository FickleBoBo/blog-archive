---
title: "[BaekJoon] 28278번 - 스택 2 [Java][C++]"
slug: baekjoon-28278
date: 2026-01-01
categories: [PS, BaekJoon]
tags: [Unlinked]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/28278)

---

## 1. 아이디어

[BaekJoon 10828번 - 스택](/posts/baekjoon-10828) 문제에서 제한만 커진 문제로 동일한 로직으로 해결할 수 있다.

---

## 2. 코드

### 1. 풀이 [Java]

```java
import java.io.*;
import java.util.*;

public class Main {

    static final int MAX = 1000000;
    static int[] stack = new int[MAX];
    static int pos = 0;

    static void push(int x) {
        stack[pos++] = x;
    }

    static int pop() {
        return stack[--pos];
    }

    static int size() {
        return pos;
    }

    static boolean empty() {
        return pos == 0;
    }

    static int top() {
        return stack[pos - 1];
    }

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();
        StringTokenizer st;

        int n = Integer.parseInt(br.readLine());
        while (n-- > 0) {
            st = new StringTokenizer(br.readLine());
            int q = Integer.parseInt(st.nextToken());

            if (q == 1) {
                int x = Integer.parseInt(st.nextToken());
                push(x);
            } else if (q == 2) {
                if (empty()) {
                    sb.append("-1\n");
                } else {
                    sb.append(pop()).append("\n");
                }
            } else if (q == 3) {
                sb.append(size()).append("\n");
            } else if (q == 4) {
                if (empty()) {
                    sb.append("1\n");
                } else {
                    sb.append("0\n");
                }
            } else {
                if (empty()) {
                    sb.append("-1\n");
                } else {
                    sb.append(top()).append("\n");
                }
            }
        }

        System.out.println(sb);
    }
}
```

### 2. 풀이 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

const int MAX = 1000000;
int st[MAX];
int pos = 0;

void push(int x) {
    st[pos++] = x;
}

void pop() {
    pos--;
}

int size() {
    return pos;
}

bool empty() {
    return pos == 0;
}

int top() {
    return st[pos - 1];
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n;
    cin >> n;

    while (n--) {
        int q;
        cin >> q;

        if (q == 1) {
            int x;
            cin >> x;
            push(x);
        } else if (q == 2) {
            if (empty()) {
                cout << -1 << '\n';
            } else {
                cout << top() << '\n';
                pop();
            }
        } else if (q == 3) {
            cout << size() << '\n';
        } else if (q == 4) {
            cout << empty() << '\n';
        } else {
            if (empty()) {
                cout << -1 << '\n';
            } else {
                cout << top() << '\n';
            }
        }
    }
}
```

---

## 3. 리뷰

없음.

---
