---
title: "[BaekJoon] 10830번 - 행렬 제곱 [Java][C++]"
slug: baekjoon-10830
date: 2025-12-12
categories: [PS, BaekJoon]
tags: [math, linear algebra, matrix exponentiation]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/10830)

---

## 1. 아이디어

행렬의 거듭제곱을 구하는 문제로 $B$ 의 크기가 최대 $100,000,000,000$ 이어서 단순한 행렬 곱셈의 반복으로는 해결할 수 없다. 행렬의 곱셈은 결합법칙이 성립한다는 점에서 이진 거듭제곱을 적용하면 해결할 수 있다.

최종 결과 행렬의 각 원소를 1,000으로 나눈 나머지를 출력해야 하는데 나머지 연산은 곱셈과 덧셈에 대한 분배 법칙이 성립해서 행렬 곱셈 과정에서 나머지 연산을 적용해주었다. 초기 행렬의 원소들도 최대 1,000 이하의 자연수여서 행렬 곱셈이 일어나지 않아도 나머지 연산을 적용해야 함에 유의해야 한다.

---

## 2. 코드

### 1. 반복문을 이용한 거듭제곱 [Java]

```java
import java.io.*;
import java.util.*;

public class Main {

    static final int MOD = 1_000;

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();
        StringTokenizer st = new StringTokenizer(br.readLine());

        int n = Integer.parseInt(st.nextToken());
        long b = Long.parseLong(st.nextToken());

        int[][] mat = new int[n][n];
        for (int i = 0; i < n; i++) {
            st = new StringTokenizer(br.readLine());
            for (int j = 0; j < n; j++) {
                mat[i][j] = Integer.parseInt(st.nextToken());
            }
        }

        int[][] res = binPow(mat, b);
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                sb.append(res[i][j]).append(" ");
            }
            sb.append("\n");
        }

        System.out.println(sb);
    }

    static int[][] binPow(int[][] mat, long n) {
        int len = mat.length;
        int[][] res = identity(len);

        while (n > 0) {
            if ((n & 1) > 0) res = multiply(res, mat);
            mat = multiply(mat, mat);
            n >>= 1;
        }

        return res;
    }

    static int[][] identity(int len) {
        int[][] id = new int[len][len];
        for (int i = 0; i < len; i++) {
            id[i][i] = 1;
        }
        return id;
    }

    static int[][] multiply(int[][] mat1, int[][] mat2) {
        int len = mat1.length;
        int[][] res = new int[len][len];

        for (int i = 0; i < len; i++) {
            for (int j = 0; j < len; j++) {
                for (int k = 0; k < len; k++) {
                    res[i][j] += mat1[i][k] * mat2[k][j];
                }
                res[i][j] %= MOD;
            }
        }

        return res;
    }
}
```

### 2. 반복문을 이용한 거듭제곱 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

using Matrix = vector<vector<int>>;

const int MOD = 1000;

Matrix identity(int len) {
    Matrix id(len, vector<int>(len));
    for (int i = 0; i < len; i++) {
        id[i][i] = 1;
    }
    return id;
}

Matrix multiply(Matrix& a, Matrix& b) {
    int len = a.size();
    Matrix res(len, vector<int>(len));

    for (int i = 0; i < len; i++) {
        for (int j = 0; j < len; j++) {
            for (int k = 0; k < len; k++) {
                res[i][j] += a[i][k] * b[k][j];
            }
            res[i][j] %= MOD;
        }
    }

    return res;
}

Matrix binpow(Matrix mat, long long n) {
    int len = mat.size();
    Matrix res = identity(len);

    while (n > 0) {
        if (n & 1) res = multiply(res, mat);
        mat = multiply(mat, mat);
        n >>= 1;
    }

    return res;
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n;
    long long b;
    cin >> n >> b;

    Matrix mat(n, vector<int>(n));
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            int x;
            cin >> x;

            mat[i][j] = x;
        }
    }

    Matrix res = binpow(mat, b);
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            cout << res[i][j] << ' ';
        }
        cout << '\n';
    }
}
```

### 3. 재귀를 이용한 거듭제곱 [Java]

```java
import java.io.*;
import java.util.*;

public class Main {

    static final int MOD = 1_000;

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();
        StringTokenizer st = new StringTokenizer(br.readLine());

        int n = Integer.parseInt(st.nextToken());
        long b = Long.parseLong(st.nextToken());

        int[][] mat = new int[n][n];
        for (int i = 0; i < n; i++) {
            st = new StringTokenizer(br.readLine());
            for (int j = 0; j < n; j++) {
                mat[i][j] = Integer.parseInt(st.nextToken());
            }
        }

        int[][] res = binPow(mat, b);
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                sb.append(res[i][j]).append(" ");
            }
            sb.append("\n");
        }

        System.out.println(sb);
    }

    static int[][] binPow(int[][] mat, long n) {
        if (n == 0) return identity(mat.length);

        int[][] half = binPow(mat, n / 2);
        if (n % 2 == 1) {
            return multiply(multiply(half, half), mat);
        } else {
            return multiply(half, half);
        }
    }

    static int[][] identity(int len) {
        int[][] id = new int[len][len];
        for (int i = 0; i < len; i++) {
            id[i][i] = 1;
        }
        return id;
    }

    static int[][] multiply(int[][] mat1, int[][] mat2) {
        int len = mat1.length;
        int[][] res = new int[len][len];

        for (int i = 0; i < len; i++) {
            for (int j = 0; j < len; j++) {
                for (int k = 0; k < len; k++) {
                    res[i][j] += mat1[i][k] * mat2[k][j];
                }
                res[i][j] %= MOD;
            }
        }

        return res;
    }
}
```

### 4. 재귀를 이용한 거듭제곱 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

using Matrix = vector<vector<int>>;

const int MOD = 1000;

Matrix identity(int len) {
    Matrix id(len, vector<int>(len));
    for (int i = 0; i < len; i++) {
        id[i][i] = 1;
    }
    return id;
}

Matrix multiply(const Matrix& a, const Matrix& b) {
    int len = a.size();
    Matrix res(len, vector<int>(len));

    for (int i = 0; i < len; i++) {
        for (int j = 0; j < len; j++) {
            for (int k = 0; k < len; k++) {
                res[i][j] += a[i][k] * b[k][j];
            }
            res[i][j] %= MOD;
        }
    }

    return res;
}

Matrix binpow(Matrix mat, long long n) {
    if (n == 0) return identity(mat.size());

    Matrix half = binpow(mat, n / 2);
    if (n % 2) {
        return multiply(multiply(half, half), mat);
    } else {
        return multiply(half, half);
    }
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n;
    long long b;
    cin >> n >> b;

    Matrix mat(n, vector<int>(n));
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            int x;
            cin >> x;

            mat[i][j] = x;
        }
    }

    Matrix res = binpow(mat, b);
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            cout << res[i][j] << ' ';
        }
        cout << '\n';
    }
}
```

---

## 3. 리뷰

없음.

---
