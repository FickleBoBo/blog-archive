---
title: "[BaekJoon] 3058번 - 짝수를 찾아라 [Java][C++]"
slug: baekjoon-3058
date: 2026-01-08
categories: [PS, BaekJoon]
tags: [Unlinked]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/3058)

---

## 1. 아이디어

<br>

주어진 7개의 수들 중 짝수의 합과 최솟값을 찾는 문제로 짝수는 2로 나눈 나머지가 0인지로 판별할 수 있다.

---

## 2. 코드

<br>

### 1. 풀이 [Java]

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();
        StringTokenizer st;

        int t = Integer.parseInt(br.readLine());
        while (t-- > 0) {
            int sum = 0;
            int min = 100;

            st = new StringTokenizer(br.readLine());
            for (int i = 0; i < 7; i++) {
                int x = Integer.parseInt(st.nextToken());

                if (x % 2 == 0) {
                    sum += x;
                    min = Math.min(min, x);
                }
            }

            sb.append(sum).append(" ").append(min).append("\n");
        }

        System.out.print(sb);
    }
}
```

<br>

### 2. 풀이 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t;
    cin >> t;

    while (t--) {
        int sum = 0;
        int mn = 100;

        for (int i = 0; i < 7; i++) {
            int x;
            cin >> x;

            if (x % 2 == 0) {
                sum += x;
                mn = min(mn, x);
            }
        }

        cout << sum << ' ' << mn << '\n';
    }
}
```

---

## 3. 디버깅

<br>

없음.

---

## 4. 참고

<br>

없음.

---
