---
title: "[BaekJoon] 14681번 - 사분면 고르기 [Java][C++]"
slug: baekjoon-14681
date: 2025-12-01
categories: [PS, BaekJoon]
tags: [geometry]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/14681)

---

## 1. 아이디어

$x$, $y$ 의 부호로 위치한 사분면을 판단할 수 있어서 조건문을 활용하면 해결할 수 있다.

- 1사분면의 경우 $x > 0$, $y > 0$
- 2사분면의 경우 $x < 0$, $y > 0$
- 3사분면의 경우 $x < 0$, $y < 0$
- 4사분면의 경우 $x > 0$, $y < 0$

---

## 2. 코드

### 1. 풀이 [Java]

```java
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        int x = Integer.parseInt(br.readLine());
        int y = Integer.parseInt(br.readLine());

        if (x > 0) {
            if (y > 0) {
                System.out.println(1);
            } else {
                System.out.println(4);
            }
        } else {
            if (y > 0) {
                System.out.println(2);
            } else {
                System.out.println(3);
            }
        }
    }
}
```

### 2. 풀이 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int x, y;
    cin >> x >> y;

    if (x > 0) {
        if (y > 0) {
            cout << 1;
        } else {
            cout << 4;
        }
    } else {
        if (y > 0) {
            cout << 2;
        } else {
            cout << 3;
        }
    }
}
```

---

## 3. 리뷰

없음.

---
