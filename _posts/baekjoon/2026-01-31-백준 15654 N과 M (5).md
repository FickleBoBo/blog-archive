---
title: "[BaekJoon] 15654번 - N과 M (5) [Java][C++]"
slug: baekjoon-15654
date: 2026-01-31
categories: [PS, BaekJoon]
tags: [Unlinked]
toc: true
math: true
---

[문제 링크](https://www.acmicpc.net/problem/15654)

---

## 1. 아이디어

$N$ 개의 자연수 중 길이가 $M$ 인 순열을 모두 구하는 문제로 재귀를 활용해서 방문 체크를 하며 구하면 된다.

---

## 2. 코드

### 1. 풀이 [Java]

```java
import java.io.*;
import java.util.*;

public class Main {

    static StringBuilder sb = new StringBuilder();
    static int n, m;
    static int[] arr;
    static int[] sel;
    static boolean[] vis;

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());

        n = Integer.parseInt(st.nextToken());
        m = Integer.parseInt(st.nextToken());

        arr = new int[n];
        st = new StringTokenizer(br.readLine());
        for (int i = 0; i < n; i++) {
            arr[i] = Integer.parseInt(st.nextToken());
        }
        Arrays.sort(arr);

        sel = new int[m];
        vis = new boolean[n];

        dfs(0);
        System.out.println(sb);
    }

    static void dfs(int sidx) {
        if (sidx == m) {
            for (int x : sel) {
                sb.append(x).append(" ");
            }
            sb.append("\n");
            return;
        }

        for (int i = 0; i < n; i++) {
            if (vis[i]) continue;

            sel[sidx] = arr[i];
            vis[i] = true;
            dfs(sidx + 1);
            vis[i] = false;
        }
    }
}
```

### 2. 풀이 [C++]

```c++
#include <bits/stdc++.h>
using namespace std;

int n, m;
int arr[8];
int sel[8];
bool vis[8];

void dfs(int sidx) {
    if (sidx == m) {
        for (int i = 0; i < m; i++) {
            cout << sel[i] << ' ';
        }
        cout << '\n';
        return;
    }

    for (int i = 0; i < n; i++) {
        if (vis[i]) continue;

        sel[sidx] = arr[i];
        vis[i] = true;
        dfs(sidx + 1);
        vis[i] = false;
    }
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n >> m;
    for (int i = 0; i < n; i++) cin >> arr[i];
    sort(arr, arr + n);

    dfs(0);
}
```

---

## 3. 리뷰

없음.

---
