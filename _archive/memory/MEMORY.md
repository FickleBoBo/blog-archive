- [blog-soft-reboot](blog-soft-reboot.md) — ★프로그래머스 SQL 102개 발행 완료(2026-07-21): br 209개 제거+제목통일+결함4건(151141괄호·298518산문·오타2). SQL은 Algo레포 원본 없음→Blog만 커밋, review Phase1 부적합(쿼리구조). baekjoon 4차 완료·잔존 2개(**5446 cnt죽은코드 / 16903 cnt설명**). push 안 함. ★2단 구조(`assets/{slug}` flat=대기, 버그아님). ⚠️커밋취소 함정: 재발행 포스트는 이미 `_posts` tracked(재발행=수정커밋, 이번 diff 6줄로 실증)라 드롭 시 옛본 남음
- [buggy-ac-verification](buggy-ac-verification.md) — AC ≠ 정답. 백준 10025 코드 4/6이 AC인데 오답(사용자 보류, BOJ 종료로 재채점 불가). ★같은 포스트 두 풀이가 다른 답 내면 하나는 틀림(해석 무관 증거). 반례는 브루트포스 실측. 제약이 막아주는 오탐 3건(2473·27436·17128)
- [tag-taxonomy](tag-taxonomy.md) — 판정 기준은 `tools/tags.md` note(중복 금지). 심판 우선순위: 발행본 grep > **solved.ac API**(선례 0일 때) > tags.md. ★solved.ac 실측 방법: puppeteer 시스템Chrome로 `/api/v3/problem/show?problemId=N` fetch(CF우회). 실측서 내 태그 다수 오류(같은 세그트리라도 12899·2243=이분탐색○/1168=✕, 2346 simulation 과다=덱만, dfs 과다). 부착툴 `tag_migrate.py attach/detach --post N --tag T --apply`(조상 자동확장)
- [code-conventions](code-conventions.md) — 관례 기준은 발행본. `tools/conventions.md` 전면 재작성 완료(더 이상 낡지 않음). ★서브에이전트가 반복 오판하는 목록: `union`/`unite`(C++ 예약어), `arr`/`v`, `max`/`mx` — 전부 정상
- [post-math-notation](post-math-notation.md) — ★산문 **변수는 수식모드 `$N$` 만장일치 관례**(발행본 546개 193:0). review_check `[MATH_VAR?]`는 오탐 아님 — 이걸 "오탐"으로 뭉개면 실수(2026-07-20 사용자 지적). 교정 시 **수식 뒤 공백 필수**(`$N$ 이`). ⚠️ 단 "숫자도 수식으로"는 관례 아님(평문 `100,000` OK, 유니첨자 `10⁵`만 위반)
- [boj-shutdown-status](boj-shutdown-status.md) — 백준 2026-04-28 종료·재오픈 미정. iCloud `baekjoon-crawling/data/problems/{번호}.json`에 원문 미러. ★solved.ac·코드포스는 puppeteer에 시스템 Chrome `executablePath` 넘기면 뚫림
